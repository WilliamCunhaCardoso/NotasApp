import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:notas_app/app/modules/nota/nota_page.dart';

main() {
  testWidgets('NotaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NotaPage(title: 'Nota')));
    //  final titleFinder = find.text('Nota');
    //  expect(titleFinder, findsOneWidget);
  });
}
